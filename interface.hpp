/**
 * imprime a tela de inicio do jogo.
 */
void imp_inicio();

/**
 * imprime os scores
 */
void imp_scores(std::string score);

/**
 * imprime o seletor de dificuldade.
 */
void imp_dificuldade();

/**
 * imprime o boneco na forca.
 */
int imp_boneco(int erros);

/**
 * imprime a tela de gameover.
 */
void imp_gameover();

/**
 * função para limpar a tela
 */
void ClearScreen();